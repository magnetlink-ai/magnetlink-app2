# 🎨 Design & Content Module — التفاصيل الكاملة

> الوحدة السابعة بنظام Magnet Link BMS. مسؤولة عن دورة حياة كل **أصل بصري أو مكتوب** للمشروع: من تسليم العميل لمواده الخام → تصميم الفريق → المراجعة والموافقة → الجاهزية والنشر.

---

## 🧭 الفكرة الجوهرية — التخزين بالـ SharePoint

**النقطة الأهم بالوحدة كلها:** الوحدة **ما عندها تخزين خاص**. كل الملفات (بوسترات، فيديوهات، لوجوهات، قوالب Word/Excel) مخزّنة فعلياً على **SharePoint**. النظام:
- **بيعرض / بيستدعي** الملفات من SharePoint ويبيّنها على شكل كروت داخل الموقع.
- **بيرفع** الملفات الجديدة لـ SharePoint (مش لقاعدة بيانات داخلية).
- كل عنصر بيحمل **مسار SharePoint (Path)** كحقل أساسي يربطه بمكانه الحقيقي.

> التوضيح: المقصود "مساحة التخزين" مش "قاعدة بيانات" — البيانات الوصفية (metadata) ممكن تكون بالنظام، بس **الملف نفسه دايماً بالـ SharePoint**.

---

## 📑 بنية الوحدة — 3 تبويبات

الوحدة صفحة وحدة بـ 3 تبويبات (Tabs). كل تبويب بيتبع لمشروع مختار (مربوط بـ Project / Client من سلكتور علوي، نفس منطق Social Media Module).

```
Design & Content
 ├── Tab 1: Design        ← أصول بصرية شغل الفريق
 ├── Tab 2: From Client   ← مواد خام واصلة من العميل
 └── Tab 3: Documents     ← قوالب وملفات محتوى مكتوب
```

---

## 🖼️ التبويب 1 — Design

أصول التصميم اللي بيشتغل عليها فريق Magnet Link لهذا المشروع.

### التصنيفات (Categories)
التبويب مقسوم داخلياً لتصنيفات. الافتراضي **3 تصنيفات** + إمكانية إضافة تصنيف رابع بأي اسم:
- **Poster**
- **Video**
- **Visual**
- *(+ تصنيف رابع اختياري — اسم حر يحدده المستخدم)*

> نفس مبدأ "قابل للتوسعة" بكل التبويبات: 3 افتراضية + رابع حسب الحاجة.

### حالات العنصر — مرحلتان

**أ) Under Modification** — العنصر لسه بطور الشغل والمراجعة.
كل عنصر بيظهر ككارت فيه:
- صورة معاينة (preview thumbnail)
- زرّين: **Approve** و **Edit**
- **Approve** → العنصر ينتقل مباشرة لمرحلة Approved.
- **Edit** → يفتح **صفحة تفاصيل العنصر** (انظر تحت).

**ب) Approved** — العناصر اللي تمّت الموافقة عليها. حالتان فرعيتان:
- **Ready** — جاهز للنشر / الانطلاق، لسه ما اننشر.
- **Published** — تم نشره فعلياً وخلص.

```
Under Modification ──(Approve)──► Approved ──► Ready ──► Published
        ▲
        └──(Edit)──► صفحة التفاصيل
```

### صفحة تفاصيل العنصر (Item Detail)
تنفتح عند الضغط على **Edit**. تعرض:
| الحقل | الوصف |
|------|-------|
| Title | عنوان العنصر |
| Status | الحالة الحالية (Under Modification / Ready / Published) |
| Company | اسم الشركة / العميل |
| Designer | المصمم اللي اشتغل عليه |
| Due Date | الموعد |
| File Type | نوع الملف (PNG, MP4, PSD, ...) |
| File Size | حجم الملف |
| SharePoint Path | المسار الكامل بالـ SharePoint |
| Comments | تعليقات — أي عضو بالفريق يقدر يضيف تعليق |

---

## 📥 التبويب 2 — From Client

كل شي **واصل من العميل** لهذا المشروع: مواد خام، لوجوهات، صور، أصول بصرية، أشكال... المصدر العميل، والوجهة الفريق يستخدمها بالتصميم.

### التصنيفات
نفس منطق Tab 1 — مقسوم لتصنيفات (Poster / Video / Visual / + رابع اختياري). مثال عملي: تصنيف "Logo" يجمع كل أشكال اللوجو اللي بعتها العميل.

### السلوك
- كل عنصر يظهر ككارت — صورة معاينة + اسم.
- **زر Download** على كل كارت → لتنزيل الملف من SharePoint.
- **رفع ملفات جديدة** (فيديوهات، صور، أصول) → ترفع لـ SharePoint.
- الضغط على الكارت → يفتح معاينة العنصر.
- إمكانية إضافة كروت جديدة لأي تصنيف.

> ما فيه دورة موافقة هون — هاي مجرد مستودع عرض للمواد الواصلة من العميل.

---

## 📝 التبويب 3 — Documents

المحتوى **المكتوب**: ملفات Word، Excel، وقوالب جاهزة. (الأفكار هون مفتوحة للنقاش — التصميم قابل للتعديل والاقتراح).

### القمّة — نفس تصميم التبويبات السابقة (هيدر + تصنيفات).

### الفكرة المحورية — Word Template
- المبرمج بيرفع **ملف Word تمبليت (Template)** جاهز بالسيستم.
- المصمم بينزّل التمبليت → بيعبّيه → بيرجع جاهز.
- التمبليت **قابل للتعديل** — المبرمج نفسه يقدر يستبدله بملف ثاني.
- الهدف: تسهيل العملية وتوحيد شكل المحتوى المكتوب.

### عناصر العنصر بهذا التبويب
كل عنصر (كارت) فيه:
- اختيار **نوع المحتوى**: Poster / Video / Category... (تصنيف)
- **صورة الشخص** اللي عمل العنصر (ينطبق رجعياً على التبويبات السابقة كمان).
- أزرار **Approve / Reject** + **تعليقات** تحتها.
- **مؤشر لوني للحالة:**
  - **أخضر** = مقبول / 100% جاهز.
  - **أحمر** = مرفوض، أو لسه بالتعديل (مش 100%).

### قابلية التوسعة
- إضافة كروت جديدة لأي تصنيف.
- إضافة **قسم/تصنيف فرعي تحتاني** باسم حر — مثلاً التبويب اسمه "Documents" بس تحته قسم اسمه "Content" خاص بنصوص الفيديوهات وهكذا.

---

## 🔗 التكامل مع باقي الوحدات

| الوحدة | نقطة الربط |
|--------|-----------|
| **Project** | كل تبويب مربوط بمشروع مختار (Project selector علوي). الأصول تخص مشروعاً محدداً. |
| **Client** | الشركة/العميل يظهر بصفحة التفاصيل (حقل Company)، ويُسحب من Client المرتبط بالمشروع. |
| **Team** | حقل Designer وصورة "الشخص اللي عمل العنصر" → يُسحبان من موظفي Team (المصدر الوحيد للأسماء). |
| **SharePoint** | (تكامل خارجي) — مصدر التخزين الفعلي لكل الملفات. النظام يتصل به للعرض والرفع والتنزيل. |
| **Permissions** | الوحدة قسم كامل ضمن نظام صلاحيات Team — تظهر/تختفي حسب صلاحية الموظف. |

**موقعها بخريطة العلاقات:**
```
Client / Project → Design & Content Module → SharePoint (تخزين الأصول)
Team (Designers)  → Design & Content (حقل المصمم + صورة المنفّذ)
```

---

## ✅ ما تم بناؤه فعلياً — تفاصيل الكود

### ثوابت الحالات

```js
// Design/Client assets
const DESIGN_STATUSES = [
  { id:'under_modification', badge:'bg-amber-500/15 text-amber-300 border-amber-500/30' },
  { id:'ready',              badge:'bg-blue-500/15 text-blue-300 border-blue-500/30' },
  { id:'published',          badge:'bg-emerald-500/15 text-emerald-300 border-emerald-500/30' },
];

// Documents فقط
const DOC_STATUS_CFG = {
  pending:  { borderColor:'border-r-amber-400',  dot:'bg-amber-400' },   // Under Review
  approved: { borderColor:'border-r-emerald-400', dot:'bg-emerald-400' },
  rejected: { borderColor:'border-r-red-400',     dot:'bg-red-400' },
};

const DC_DEFAULT_CATS = ['Poster','Video','Visual'];
```

**ملاحظة على الحالات:** في Documents تبويب الحالات مختلفة (pending/approved/rejected). في Design/Client assets الحالات هي under_modification/ready/published. لا يوجد حالة "Approved" منفصلة بـ Design — الزر "Approve" ينقل مباشرة لـ `ready`.

### SharePoint MSAL Integration (مُنجَز)

```js
const SP_SCOPES = [
  'https://graph.microsoft.com/Sites.ReadWrite.All',
  'https://graph.microsoft.com/Files.ReadWrite'
];
```

**تدفق التوثيق:**
- `handleSpLogin()` → MSAL `loginPopup({ scopes: SP_SCOPES })` → يخزّن `spUser`
- `handleSpLogout()` → `logoutPopup()`
- **Auto-restore:** على `mount` يفحص `msal.getAllAccounts()` → لو فيه جلسة سابقة يستعيدها
- **Auto-sync:** كلما تغيّر `spUser` أو `selProjectId` → `handleSpSync()` تلقائياً

**إعداد مجلدات المشروع:**
`handleSetupFolders()` → ينشئ هيكل مجلدات في SharePoint بناءً على اسم المشروع:
```js
getSpPaths(projectName) → { design: { root, cats }, client: { root, cats }, docs: { root, cats } }
```
يُنظّف اسم المشروع من الرموز الخاصة (regex): `[\?\%*:|"<>]` → `-`

### التصنيفات

- افتراضي: `DC_DEFAULT_CATS = ['Poster','Video','Visual']`
- Documents يضيف تلقائياً: `'Content'` (+ الافتراضية)
- `customCats` state: `{ design:[], client:[], docs:[] }` قابل للتوسيع

### Drag & Drop

- `dragInfo`, `dragOverTab`, `dragOverCat` states
- يمكن سحب assets بين التصنيفات (drag & drop بين categories)

## 🆕 تحسينات السرعة والوضوح — مطبّقة

**المشكلة اللي انحلّت:** الوحدة كانت حاسّة "ضايعة وبطيئة" — عناصر جديدة ما تظهر إلا بعد تحديث يدوي، تصنيفات فاضية تختفي بالكامل (فمافي مكان واضح تضيف فيه)، وما فيه تعديل كامل على أصل بعد إضافته غير الاسم والحالة.

1. **تحديث فوري بعد الإضافة** — بعد ما ترفع أصل/ملف/وثيقة، `loadCurrentTab()` تنعاد تلقائياً فتظهر فوراً بدون الحاجة للضغط على زر التحديث (⟳).
2. **تحميل أسرع** — جلب ملفات التصنيفات الفرعية من SharePoint صار **متوازي** (`Promise.all`) بدل التسلسلي.
3. **كل التصنيفات ظاهرة دايماً** — حتى لو فاضية، مع رسالة "لا توجد ملفات بهذا التصنيف بعد" + زر **+ سريع** بجانب اسم كل تصنيف يفتح نموذج الإضافة بنفس التصنيف محدد مسبقاً.
4. **حذف تصنيف مخصّص** — أي تصنيف رابع (مو من الافتراضية Poster/Video/Visual/Content) يظهر إله زر **×** بجانبه بس لو فاضي من الملفات، لمنع فقدان ملفات بالغلط.
5. **تعديل كامل من صفحة التفاصيل** — Category / Designer (أو منفّذ الوثيقة) / Due Date صاروا قابلين للتعديل مباشرة (Inline، نفس أسلوب CRM) من داخل Overlay التفاصيل — مو بس الاسم والحالة متل قبل. تغيير التصنيف من هون يستخدم نفس منطق النقل الآمن لـ SharePoint (`handleMoveCat`) فينقل الملف فعلياً للمجلد الصحيح.
   - تبويب **From Client** ما بيعرض حقلي Designer/Due Date لأنه ما إلهم معنى بهاي التبويب (بدون دورة موافقة أصلاً).
6. **توحيد اللغة** — رسائل "اربط حسابك"، "جاري التحميل"، وعناوين جدول متصفّح SharePoint صارت عبر `t()` بدل نص عربي ثابت، فتترجم صح بوضع اللغة الإنجليزية.

## ❓ نقاط مفتوحة (متبقية)

1. **التعليقات** — مين يقدر يضيف/يحذف؟ ومربوطة بصلاحيات Team؟
2. **SharePoint Site URL** — يُضبط عبر `spRootPath` input — يحتاج إدخال يدوي من المستخدم.
3. **إعادة تسمية تصنيف** — مو مدعومة بعد (بس إضافة/حذف) لأنها بتحتاج نقل فعلي لمجلد SharePoint بالكامل.
